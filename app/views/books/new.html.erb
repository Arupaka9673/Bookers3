<h1>新規投稿</h1>

<%= link_to "投稿一覧へ戻る", book_path(@book.id) %>

<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Body</h4>
  <%= f.text_area :body %>
	
  <%= f.submit '投稿' %>
<% end %>

